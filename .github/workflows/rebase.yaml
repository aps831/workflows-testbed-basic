---
name: Git Branch Rebase
on:
  issue_comment:
    types:
      - created
permissions:
  contents: write
jobs:
  squash:
    uses: aps831/workflows/.github/workflows/rebase.yaml@feat/rebase
    secrets:
      ACCESS_TOKEN: ${{ secrets.GITHUB_TOKEN }}
